#include <iostream>
#include <stdlib.h>
#include "Nodo.cpp"

using namespace std;

int main(int argc, char const *argv[]) {
    Nodo *head = new Nodo(3);
    Nodo *seg1 =
    Nodo *seg2 =
    Nodo *seg_fin
    cout << head->n << endl;

    return 0;
}
